Just add the following line to your .bashrc / .zshrc

```
alias tangle-out-main="emacs --batch -l org main.org -f org-babel-tangle or emacs --batch -l org --eval '(org-babel-tangle-file \"main.org\")'"
```

replace main.org with the name of the file you want to tangle code blocks out from.